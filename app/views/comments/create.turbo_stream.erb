<%= turbo_stream.prepend "comments", partial: "comments/comment", locals: { comment: @comment } %>
<%= turbo_stream.replace "comment_form", partial: "comments/form", locals: { review: @review, comment: @review.comments.build } %>
